// You are given n number of bulbs. 
// They are all switched off. 
// A weird fluctuation in voltage hits the circuit n times.
// In the 1st fluctuation, all bulbs are toggled,
// in the 2nd fluctuation every 2nd bulb is toggled, 
// in the 3rd fluctuation, every 3rd bulb is toggled, and so on.
// You've to find which bulbs will be switched on after n
// fluctuations.
// void bulb(bool a[],int n,int j){
//     for(int i=j-1;i<n;){
//         if(a[i]==false)a[i]=true;
//         else if(a[i]==true)a[i]=false;
//         i+=j;
//     }

// }
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;cin>>n;
    // bool a[n];
    // for(int i=0;i<n;i++){
    //     a[i]=true;
    // }
    // for(int i=2;i<=n;i++){
    //     bulb(a,n,i);
    // }
    // for(int i=0;i<n;i++){
    //     if(a[i]==true) cout<<"b"<<i+1<<" ";
    // }
}